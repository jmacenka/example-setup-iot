#/bin/sh
# Script to do preparation steps for terraform deployment
# If your SSH-Key is encrypted (which it sould be!), it needs to be passed to the agent to be consumed by the automation pipeline

ssh-add ~/.ssh/id_jan_SHS_ed25519